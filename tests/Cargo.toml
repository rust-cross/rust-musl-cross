[package]
authors = ["root"]
name = "test-musl"
version = "0.1.0"

[dependencies]
env_logger = "0.9"
error-chain = "0.12"
reqwest = { version = "0.11", features = ["blocking", "native-tls-vendored"] }

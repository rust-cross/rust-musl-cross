[package]
authors = ["root"]
name = "test-musl"
version = "0.1.0"

[dependencies]
env_logger = "0.8"
error-chain = "0.12"
openssl-probe = "0.1"
reqwest = { version = "0.10", features = ["blocking"] }
